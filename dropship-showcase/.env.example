# ── Frontend Environment Variables ─────────────────────────────────────────────
# Leave unset when running via `npm run dev` — the Vite dev proxy handles /api -> backend.
# Set this for production deployments where frontend and backend are on different domains.
# VITE_API_URL=https://your-backend-domain.com/api
